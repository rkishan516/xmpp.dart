name: xmpp_test
description: Test utilities for XMPP Dart packages
version: 0.1.0

resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  xmpp_events: ^0.1.0
  xmpp_jid: ^0.1.0
  xmpp_xml: ^0.1.0
  xmpp_client: ^0.1.0

dev_dependencies:
  test: ^1.24.0
  lints: ^6.0.0
