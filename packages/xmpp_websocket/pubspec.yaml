name: xmpp_websocket
version: 0.1.0
description: WebSocket transport for XMPP connections.

resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  xmpp_events: ^0.1.0
  xmpp_xml: ^0.1.0
  xmpp_connection: ^0.1.0
  web_socket_channel: ^3.0.0

dev_dependencies:
  test: ^1.24.0
  lints: ^6.0.0
