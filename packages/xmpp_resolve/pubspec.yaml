name: xmpp_resolve
version: 0.1.0
description: DNS SRV resolution for XMPP service discovery.

resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  xmpp_events: ^0.1.0

dev_dependencies:
  test: ^1.24.0
  lints: ^6.0.0
