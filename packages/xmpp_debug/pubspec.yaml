name: xmpp_debug
description: Debug logging for XMPP connections using Talker logger
version: 0.1.0

resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  talker: ^5.1.2
  xmpp_events: ^0.1.0
  xmpp_xml: ^0.1.0

dev_dependencies:
  test: ^1.24.0
  lints: ^6.0.0
