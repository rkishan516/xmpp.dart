name: xmpp_base64
description: Base64 encoding/decoding utilities for XMPP
version: 0.1.0
repository: https://github.com/xmppjs/xmpp.dart/tree/main/packages/xmpp_base64

resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"

dev_dependencies:
  test: ^1.24.0
  lints: ^6.0.0
