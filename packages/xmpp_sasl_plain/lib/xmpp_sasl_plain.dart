/// SASL PLAIN mechanism for XMPP authentication.
///
/// The PLAIN mechanism sends credentials in plain text (base64 encoded).
/// Should only be used over TLS connections.
library;

export 'src/plain.dart';
