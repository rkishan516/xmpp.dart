name: xmpp_component_core
description: Core XMPP component implementation for Dart (XEP-0114)
version: 0.1.0

resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  xmpp_connection: ^0.1.0
  xmpp_events: ^0.1.0
  xmpp_jid: ^0.1.0
  xmpp_xml: ^0.1.0
  crypto: ^3.0.3

dev_dependencies:
  test: ^1.24.0
  lints: ^6.0.0
